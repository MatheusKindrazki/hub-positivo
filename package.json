{
  "name": "psdhub",
  "private": true,
  "version": "2.7.1",
  "repository": {
    "type": "git",
    "url": "https://editorati.visualstudio.com/Produtos%20Digitais/_git/hub-digital-front"
  },
  "author": {
    "email": "matheussaldanha@positivosolucoes.com.br",
    "name": "Matheus Kindrazki"
  },
  "badges": [
    "hub",
    "novo-on",
    "positivo-on"
  ],
  "workspaces": {
    "packages": [
      "packages/*",
      "packages/apps/*",
      "packages/libs/*"
    ]
  },
  "scripts": {
    "test": "jest -u --colors --logHeapUsage --maxWorkers=4",
    "test:watch": "jest  -u --colors --watch --collectCoverage --logHeapUsage --maxWorkers=4",
    "test:watch:inspect": "jest --colors --logHeapUsage --maxWorkers=4",
    "test:coverage": "jest -u --colors --silent --collectCoverage --testResultsProcessor=jest-sonar-reporter --maxWorkers=4 && coverage-badges",
    "posttest:coverage": "svg2png --input './docs/**/*.svg' --output ./docs/icons --format png",
    "lint": "eslint --ext .tsx,.ts, packages/**/src",
    "build:easyauth": "npm run --prefix packages/apps/easyauth build",
    "prebuild:web": "npm run --prefix packages/libs/helpers build",
    "build:web": "npm run --prefix packages/apps/web build",
    "postbuild:web": "node scripts/replace-in-files.js hub",
    "build:common": "npm run --prefix packages/common build",
    "postinstall": "npm run --prefix packages/libs/helpers build",
    "release": "HUSKY_SKIP_HOOKS=1 semantic-release --branches main",
    "commit": "git-cz"
  },
  "devDependencies": {
    "@commitlint/cli": "^11.0.0",
    "@commitlint/config-conventional": "^11.0.0",
    "@newrelic/publish-sourcemap": "^5.1.0",
    "@psdhub/eslint": "*",
    "@semantic-release/changelog": "^5.0.1",
    "@semantic-release/git": "^9.0.0",
    "@testing-library/react-hooks": "^5.0.3",
    "@types/jest": "^26.0.20",
    "commitizen": "^4.2.3",
    "coverage-badges": "^1.0.4",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "~4.3.0",
    "jest": "^26.6.3",
    "jest-matchmedia-mock": "^1.1.0",
    "jest-sonar-reporter": "^2.0.0",
    "jest-svg-transformer": "^1.0.0",
    "lint-staged": "^10.5.4",
    "react-select-event": "^5.2.0",
    "replace-in-files": "^3.0.0",
    "semantic-release": "^17.3.8",
    "semantic-release-monorepo": "^7.0.4",
    "svg-png-converter": "^0.0.9",
    "ts-jest": "^26.5.1",
    "typescript": "4.2.4"
  },
  "dependencies": {
    "@emotion/cache": "^11",
    "@emotion/react": "^11"
  }
}
