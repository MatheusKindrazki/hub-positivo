import {
  Briefcase,
  ClipboardText,
  BookBookmark,
  Users,
  Pencil,
} from 'phosphor-react';

const types = {
  gestor: Briefcase,
  administrador: Briefcase,
  coordenador: ClipboardText,
  professor: Pencil,
  aluno: BookBookmark,
  familia: Users,
};

export default types;
