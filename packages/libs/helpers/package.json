{
  "name": "@psdhub/helpers",
  "version": "1.2.2",
  "main": "src/index.ts",
  "author": "matheuskindrazki",
  "description": "Communicator end-to-end between solutions and PSD hub",
  "license": "MIT",
  "engines": {
    "node": "^10.12.0 || >=12.0.0"
  },
  "scripts": {
    "prebuild": "rm -rf build",
    "build": "tsc -p .",
    "postbuild": "cp package.json build && node scripts/update-json.js"
  },
  "files": [
    "*"
  ],
  "dependencies": {},
  "devDependencies": {}
}
