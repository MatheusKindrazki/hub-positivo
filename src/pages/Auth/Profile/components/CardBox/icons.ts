import { Briefcase, Clipboard, BookBookmark, Users } from 'phosphor-react';

const types = {
  gestor: Briefcase,
  professor: Clipboard,
  aluno: BookBookmark,
  familia: Users,
};

export default types;
