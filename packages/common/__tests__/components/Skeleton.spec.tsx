import React from 'react'

import Skeleton, {
  SkeletonProps,
  SkeletonTextProps,
  SkeletonCircle,
  SkeletonText,
  Stack
} from '../../components/Skeleton'

describe('Skeleton component should work properly', () => {
  it('init', () => {
    expect(1).toBe(1)
  })
})
