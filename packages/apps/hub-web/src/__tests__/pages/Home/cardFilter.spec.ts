import { cardFilter } from '~/pages/Home/cardFilter'

import { products } from './mockState.json'

describe('testing cardFilter functions', () => {
  it('getting started', () => {
    const { data } = products
  })
})
